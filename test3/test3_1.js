/**
 * [3-1]
 * 以下の関数を作成して、作成しした関数を呼び出してください。
 * 呼び出す際に引数に 1 を渡し、その戻り値をコンソールログに表示してください。
 * ・仮引数の値を10倍にして呼び出し元に返してください。
 */
function main() {
    console.log(tenTimeMethod(1));
}

function tenTimeMethod(value) {
    return value * 10;
}
